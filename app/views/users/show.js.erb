$("#change_view").html("<%= escape_javascript(render partial: 'view_btn', locals: { monthly_view: @monthly_view } ) %>");

$("#pie_graph").html("<%= escape_javascript(render partial: 'pie_graph', locals: { monthly_view: @monthly_view } )  %>");
